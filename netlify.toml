[build]
  # 构建命令
  command = "pnpm build"
  # 构建输出目录（Vite 默认输出到 dist）
  publish = "dist"

# SPA 路由重定向规则（支持 Vue Router history 模式）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 环境变量（如果需要，可以在 Netlify 控制台设置）
# [build.environment]
#   NODE_VERSION = "18"

