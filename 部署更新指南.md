# 更新部署到 Netlify 指南

## 📋 前提条件

1. ✅ 已修改 GitHub 链接
2. ✅ 已安装 Git
3. ✅ 已有 GitHub 账号
4. ✅ 已在 Netlify 创建站点（或准备创建）

---

## 🚀 方法一：通过 Git 自动部署（推荐）

### 步骤 1: 初始化 Git 仓库

```bash
# 进入项目目录
cd /Users/lee6583/Downloads/llm-chat-box2.0-main

# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 提交更改
git commit -m "Update GitHub link and deploy to Netlify"
```

### 步骤 2: 在 GitHub 创建仓库

1. 访问 [GitHub](https://github.com/new)
2. 创建新仓库，名称：`llm-chat-box`
3. **不要** 初始化 README、.gitignore 或 license（项目已有）
4. 点击 "Create repository"

### 步骤 3: 连接并推送到 GitHub

```bash
# 添加远程仓库（替换 YOUR_USERNAME 为你的 GitHub 用户名）
git remote add origin https://github.com/lee6583/llm-chat-box.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 步骤 4: 在 Netlify 连接仓库

如果还没有在 Netlify 创建站点：

1. 访问 [Netlify](https://app.netlify.com)
2. 点击 "Add new site" → "Import an existing project"
3. 选择 "GitHub"
4. 授权并选择仓库 `llm-chat-box`
5. 配置构建设置（会自动检测 `netlify.toml`）：
   - **Build command**: `pnpm build`
   - **Publish directory**: `dist`
6. 点击 "Deploy site"

如果已经创建了站点：

1. 进入站点设置
2. 在 "Site settings" → "Build & deploy" → "Continuous Deployment"
3. 确保已连接正确的 GitHub 仓库
4. 每次推送到 `main` 分支会自动触发部署

---

## 🔧 方法二：使用 Netlify CLI 手动部署

### 步骤 1: 安装 Netlify CLI（如果还没安装）

```bash
npm install -g netlify-cli
```

### 步骤 2: 登录 Netlify

```bash
netlify login
```

### 步骤 3: 部署

```bash
# 构建项目
pnpm build

# 部署到生产环境
netlify deploy --prod

# 或先部署到预览环境
netlify deploy
```

---

## 📝 方法三：拖拽部署（快速测试）

### 步骤 1: 构建项目

```bash
pnpm build
```

### 步骤 2: 拖拽部署

1. 访问 [Netlify Drop](https://app.netlify.com/drop)
2. 将 `dist` 文件夹拖拽到页面
3. 等待部署完成

**注意**: 此方法不会自动更新，每次更新都需要重新拖拽。

---

## ✅ 验证部署

部署完成后：

1. 访问你的 Netlify 站点 URL（例如：`https://llm-chat-box.netlify.app`）
2. 检查右上角的 GitHub 图标链接是否正确
3. 点击 GitHub 图标，确认跳转到你的仓库

---

## 🔄 后续更新流程

一旦设置了 Git + Netlify 自动部署，后续更新非常简单：

```bash
# 1. 修改代码后，提交更改
git add .
git commit -m "描述你的更改"

# 2. 推送到 GitHub
git push

# 3. Netlify 会自动检测并部署（通常 1-2 分钟）
```

---

## 🐛 常见问题

### 问题 1: 构建失败

**解决方案**:
- 检查 `package.json` 中的依赖是否正确
- 确保 Node.js 版本兼容（建议 18+）
- 查看 Netlify 构建日志排查错误

### 问题 2: GitHub 链接未更新

**解决方案**:
- 清除浏览器缓存
- 检查部署是否成功
- 确认代码已正确提交和推送

### 问题 3: 自动部署未触发

**解决方案**:
- 检查 Netlify 站点设置中的 Git 连接
- 确认推送到了正确的分支（通常是 `main`）
- 在 Netlify 控制台手动触发部署

---

## 📚 相关文档

- [Netlify 部署文档](https://docs.netlify.com/)
- [Git 基础教程](https://git-scm.com/book/zh/v2)
- [GitHub 使用指南](https://docs.github.com/)

